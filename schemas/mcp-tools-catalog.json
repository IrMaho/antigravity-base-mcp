[
  {
    "name": "echo",
    "description": "Echoes back the input message with optional repetition and prefix formatting.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "message": {
          "description": "The text message to echo back",
          "type": "string"
        },
        "repeat": {
          "description": "Number of times to repeat the message",
          "type": "number"
        },
        "prefix": {
          "description": "Optional prefix to prepend to each line",
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  },
  {
    "name": "get_system_info",
    "description": "Retrieves current system diagnostics including OS, Node version, memory, and CPU info.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "includeMemory": {
          "description": "Whether to include memory statistics",
          "type": "boolean"
        },
        "includeCpu": {
          "description": "Whether to include CPU architecture details",
          "type": "boolean"
        }
      },
      "additionalProperties": false
    }
  },
  {
    "name": "my_custom_tool",
    "description": "Detailed description of what this tool accomplishes, its expected input, and its output format.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "description": "Search query or target input string",
          "type": "string"
        },
        "limit": {
          "description": "Maximum number of items to return",
          "type": "number"
        },
        "options": {
          "description": "Optional execution configuration",
          "type": "object",
          "properties": {
            "detailed": {
              "description": "Include verbose output",
              "type": "boolean"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  }
]